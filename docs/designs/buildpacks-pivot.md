# Design: cv_oci — pivot to Cloud Native Buildpacks

Generated by /office-hours on 2026-09-01
Reviewed by /plan-eng-review on 2026-09-01 (16-finding outside voice; scope
decoupled — see Eng review decisions)
Branch: main
Repo: InSuperposition/cv_oci
Status: ACTIVE — plan of record for cv_oci. Approved 2026-09-01; revised by
`/plan-eng-review` (SCOPE_REDUCED) and `/plan-ceo-review` (HOLD_SCOPE) the same
day. Implementation in progress.
Mode: Builder
Supersedes: `docs/designs/pipeline-restructure.md`,
`docs/designs/pipeline-gitops-replan.md` (both now carry SUPERSEDED headers), and
the /office-hours draft `tensor-main-design-20260829-155358.md`. Still-valid
content from the first two is mapped in "Relationship to the old slice plan"
below and will be merged into one SSOT after Commit 1b.

## Problem Statement

cv_oci builds `cv_frontend` into a signed, provenance-bearing OCI image using
Tekton + `crane` + `apko` + ~1530 lines of bash, under a self-imposed "no
Dockerfile, no Docker daemon" constraint. An adversarial review judged the repo
over-engineered: it hand-rolls with assembly-language primitives what Cloud
Native Buildpacks does with a lifecycle. The user retired the no-Dockerfile
constraint on 2026-09-01 (the lesson it taught is learned) and decided to rebuild
the image-production half on CNB, keeping the supply-chain curriculum (in-cluster
build, SBOM, signing, verify, GitOps).

**Scope (post-eng-review):** cv_oci's job is the pipeline — build `cv_frontend`
with a **stock Paketo builder**, deploy by digest, then layer the supply-chain
slices (least-privilege SAs, determinism, CVE + SBOM, Chains signing, multi-arch,
promotion, GitOps). The hand-authored `cv/*` buildpack suite is **a separate
project** (`packs`), decoupled from cv_oci's history — see Deferred.

Bisect-safe commit history and correct naming stay hard constraints.

## What Makes This Cool

A commit-by-commit supply-chain curriculum where each commit demonstrates exactly
one subsystem and the pipeline stays green: `cv_frontend@SHA → stock CNB build →
signed OCI image + SLSA provenance + SBOM in zot → verified → reconciled by
digest`. The git history is the portfolio artifact. CNB replaces ~1530 lines of
bash with a lifecycle you configure, so the curriculum is about the *supply
chain*, not about hand-rolling OCI plumbing.

## Constraints

- **Learning + portfolio** (`cv_` prefix, public repo). Optimize for "each commit
  demonstrates one subsystem," not delivery speed.
- **Two repos in scope:** `cv_oci` (pipeline) and `cv_frontend` (app,
  `../frontend`, `remix@3.0.0-beta.10`, Node `>=24.3.0`, built by pinned commit
  SHA). `packs` is a separate follow-on project, not on cv_oci's critical path.
- **Local dev:** OrbStack Kubernetes on Apple Silicon, `v1.35.6+orb1`,
  cri-dockerd, **arm64 only, single node**. Tekton Pipelines v1.15.1 installed.
- **Native arch only, no QEMU** (standing rule, kept).
- **Builder = `heroku/builder:24`** (probed 2026-09-01). Every
  `paketobuildpacks/builder-*` on Docker Hub is amd64-only; Heroku's is genuinely
  multi-arch. Below, "Paketo" as a noun still names the *concept* the deferred
  `cv_packs` suite mirrors; the running cv_oci builder is Heroku's. Node support
  is the monolithic `heroku/nodejs` buildpack (detect + install + launch in one).
- **Reuse the existing stack.** zot as the registry. cosign, Tekton Chains, Flux,
  Timoni stay in the curriculum. No new controllers (no kpack).
- **`cv_frontend` has no `npm run build`.** `package.json` scripts: `dev`, `hmr`,
  `start`, `test`, `typecheck`. `start` is
  `NODE_ENV=production node --import remix/node-tsx server.ts`. Remix 3 beta
  compiles browser assets on demand via esbuild at request/startup time; there is
  no ahead-of-time asset build.
- **"Keep it simple. We can fix things at a later date."** — user, this session.

## Premises

1. **Use the stock Paketo Node.js builder.** It already does Node resolution,
   lockfile-driven `npm ci`, SBOM (Syft/SPDX/CycloneDX), reproducibility. cv_oci
   configures it; it does not re-author it. (Accepted — the `cv/*` suite that
   would re-derive Paketo is decoupled to `packs`.)
2. **`cv_frontend` builds with `node-engine` + `npm-install` + `npm-start` only.**
   No `npm run build` → the build buildpack no-ops. This is fine — Paketo handles
   it. (Accepted.)
3. **CNB does not shrink the runtime image for Remix 3.** esbuild/oxc/lightningcss
   are needed at request/startup regardless of build tool. The "Option A"
   compromise (`debt.md`) survives the pivot unchanged. CNB removes hand-rolled
   `crane`/`apko`; it does not remove the compiler. (Accepted.)
4. **The app-image build is daemonless and in-cluster** — the Tekton `buildpacks`
   0.6 task running `/cnb/lifecycle/creator`, not kpack, not `pack build`.
   (Accepted, arm64 verified at gate zero.)
5. **Deploy by digest.** The `cv` Deployment receives `<zot>/cv@sha256:…`, never a
   tag. (Accepted.)

## CEO review decisions (2026-09-01, HOLD SCOPE)

- **Decouple confirmed.** `cv_packs` stays deferred (repo exists, TODOS.md entry,
  no committed timeline) — the CNB-authoring lesson is kept as an open door, not
  smuggled back into cv_oci via a "capstone" swap. cv_oci uses the stock Paketo
  builder permanently.
- **cv_oci is now a supply-chain curriculum, not a build-tooling curriculum** —
  own this in the README. The portfolio story is "signed supply chain on standard
  tools," not "no-Dockerfile OCI pipeline from primitives."
- **Demote the old design docs in Commit 0**, not Commit 1a — the moment gate
  zero passes, `pipeline-restructure.md` labeled "sole source of truth" misleads
  any repo reader. Commit 0 is a docs-only cv_oci commit (gate-zero record + two
  `Status: SUPERSEDED` headers).
- **Go build.** Three planning passes today; the plan is done. Gate zero (T2) is
  the next action, not a fourth review.

## Eng review decisions (2026-09-01, folded in)

1. **Suite decoupled.** The `cv/*` buildpack suite (originally Commits 2-5) is
   pulled out of cv_oci entirely. It repeats, one level up, the over-engineering
   the pivot cures ("hand-roll what Paketo ships" vs "hand-roll what CNB does"),
   and it showcases the three trivial buildpacks. cv_oci uses the stock Paketo
   builder, pinned by digest. `packs` becomes its own project with its own
   history, consuming `cv_frontend` as one fixture — never pinned into cv_oci's
   `BUILDER_IMAGE`. This removes the two-repo bisect coupling and the zot-as-SPOF
   problem in one move.
2. **Gate zero builds `cv_frontend@SHA` directly** — a cut-down Commit 1, not a
   `hello-node` toy. The real risks (no `npm run build`, request-time esbuild,
   `optionalDependencies` surviving Paketo's launch-layer prune, the deployment
   security context, `/healthz`) all live in `cv_frontend`; the probe must hit
   them.
3. **`resolve` Task folded** into the `buildpacks` task's own git fetch + a tiny
   pre-step for SHA validation + `frontend-contract.md` assertions.
4. **Tekton task is `buildpacks` 0.6** (`tektoncd/catalog task/buildpacks/0.6`).
   Vendored + every step image digest-pinned. Its `prepare` step runs `chown -R`
   as **root** → a documented root exception (`debt.md`); the pipeline namespace
   must not enforce `restricted` PSA on that step (it already runs `privileged`
   for the zot seed per the replan). Its `create` step hardcodes
   `runAsUser: 1000` (Paketo matches). Registry auth = the task's `dockerconfig`
   workspace (mount a Secret).
5. **Commit 1 splits 1a / 1b.** 1a adds the CNB pipeline alongside the old one
   (both green, `validate.sh` covers both) and demotes the old design docs. 1b
   deletes the crane/apko pipeline + `pipeline-utils` + old tests. Two clean
   bisect points.
6. **No build cache in Commit 1** — `SKIP_RESTORE=true`, no `CACHE_IMAGE`, the
   `cache` workspace is an `emptyDir`. Matches `debt.md` ("npm ci runs cold every
   build"). Add a zot `CACHE_IMAGE` only when build time is measured as a
   problem.
7. **Pin the Node version.** `engines.node: ">=24.3.0"` + Paketo resolution can
   pull Node 25. Tighten `cv_frontend`'s `engines.node` to `~24.3` (or set
   `BP_NODE_VERSION=24.*` in the pipeline `ENV_VARS`) so the runtime stays Node
   24.x, matching `frontend-contract.md`. This is a `cv_frontend` commit + a
   contract-doc bump, prerequisite to gate zero.
8. **Chains provenance is task-authoring work, not just a probe.** The
   `source → buildpack digests → builder digest → run-image digest → app digest`
   chain needs the `buildpacks` task to emit those digests as results with type
   hints (the old plan emitted `*_ARTIFACT_INPUTS` for every base/tool digest
   explicitly). Scope it into the Chains slice, not a one-liner.
9. **If Paketo arm64 fails gate zero, STOP and rethink** — do not "switch to
   Heroku." Heroku's nodejs buildpack is monolithic; it does not decompose into
   `node-engine`/`npm-install`/`npm-start` and would need a different pipeline
   shape. Approach C (hand-rolled lifecycle Task) is the real fallback.

## Cross-Model Perspective

### office-hours: Codex cold read

- **Coolest version:** the "buildpack microscope" (diff `cv/*` against the stock
  lane). → moved to the `packs` project.
- **What excites the user most:** *"bisect safe, correct naming conventions."*
- **Existing 50%:** Paketo Node.js buildpacks. cv_oci's remaining work is the
  slices, not the buildpacks.

### eng review: outside voice (Claude subagent — Codex timed out)

16 findings. The scope verdict — "Commits 0-1 + slice migration is the real
differentiated portfolio piece; the `cv/*` suite repeats the over-engineering the
pivot was meant to cure; decouple it" — was adopted (Decision 1). Also folded:
the Node-25 resolution gap (7), Chains-authoring scope (8), the Heroku-fallback
error (9), gate-zero fixture (2), Commit 1 split (5), demoting
`pipeline-restructure.md` early (Commit 1a), cutting the `packs` GHA mirror.

## Approaches Considered

### Approach A: Stock Paketo builder + incremental slices (chosen)

Gate zero builds `cv_frontend@SHA` with heroku/builder:24 (arm64). Commit 1
(a/b) is the reference lane + old-pipeline demolition. Then the supply-chain
slices layer on, each its own bisect-safe commit, pipeline green throughout.
Effort M, risk Low.

### Approach C: Hand-rolled lifecycle Task (fallback only)

A ~30-line Task runs `/cnb/lifecycle/{detector,analyzer,restorer,builder,exporter}`
explicitly. Adopted only if gate zero shows the stock Paketo builder cannot run
on arm64 OrbStack. More YAML to own; also a legitimate "lifecycle phases" lesson
if you want it later.

### Rejected: the `cv/*` buildpack suite in cv_oci's history

Re-deriving Paketo's `node-engine`/`npm-install`/`npm-start`, one per commit,
coupled to cv_oci's `BUILDER_IMAGE`. Rejected: repeats the over-engineering,
showcases the trivial buildpacks, and couples cv_oci's bisect-safety to an
ephemeral single-node zot. Moved to `packs` as a standalone project.

## Recommended Approach

Approach A. Gate zero (cv_frontend@SHA) → Commit 1a (CNB pipeline alongside old)
→ Commit 1b (delete old) → the slices.

### Build execution

- **Vendor `tektoncd/catalog task/buildpacks/0.6`** into `cv_oci`
  (`tasks/buildpacks.yaml`). Pin `docker.io/library/bash:5.1.4@sha256:b208215a…`
  and the builder param by `@sha256`. `create` runs `/cnb/lifecycle/creator`.
  Workspaces: `source`, `cache` (emptyDir), `dockerconfig` (Secret). Results:
  `APP_IMAGE_DIGEST`, `APP_IMAGE_URL`. The `tekton.dev/platforms: linux/amd64`
  annotation is advisory, not enforced.
  - `prepare` chowns as root — vendor it with a documented root exception for
    that step (`debt.md`, mirrors the retired `assemble` docker-load exception).
    Confirm the pipeline namespace's PSA level permits it (the replan already
    sets `privileged` for the zot namespace; the pipeline namespace needs the
    same or a targeted exception).
  - `create` hardcodes `runAsUser: 1000` — the Paketo builder's `CNB_USER_ID` is
    1000, so this matches. Record it.
- **Reference builder: `heroku/builder:24`.** Probed 2026-09-01: every
  `paketobuildpacks/builder-*` image on Docker Hub is **amd64-only**
  (`builder-jammy-*`, `builder-ubi8-base`); `builder-noble-*` / `builder-ubi9-*`
  do not exist. The office-hours claim that Paketo ships arm64 builders did not
  survive a live check. `heroku/builder:24` is genuinely multi-arch — arm64
  child `sha256:e74d36e0e70f9b588f62483bef73a51809c1f51f04103dbb323293261bfc43aa`,
  lifecycle 0.21.18 (platform API 0.7–0.15, task default 0.9), run image
  `heroku/heroku:24` (arm64 present). Its Node support is the **monolithic**
  `heroku/nodejs` buildpack (not decomposed into `node-engine`/`npm-install`/
  `npm-start` the way Paketo is). For cv_oci that is fine — it needs a working
  CNB build, and `heroku/nodejs` does detect + install + launch. The monolith
  only matters for `cv_packs` (which can no longer mirror a Paketo decomposition
  the reference lane does not have — noted in the TODO). Pin the arm64 child
  digest in `digests.cue`. Native arm64 build on OrbStack, no QEMU.
- **Node version pin:** set `BP_NODE_VERSION` (or rely on a tightened
  `cv_frontend` `engines.node: ~24.3`) so the runtime is Node 24.x.
- **No build cache** in Commit 1 (`SKIP_RESTORE=true`).

### Gate zero (Commit 0, throwaway TaskRun — not committed to `cv_oci` `main`)

Run the vendored `buildpacks` 0.6 task with heroku/builder:24 (arm64) against
`cv_frontend` at the pinned fixture SHA, on the arm64 node. **Assert:**
`creator` exits 0; the SBOM is emitted; `optionalDependencies` (esbuild, oxc,
lightningcss arm64 binaries) are present in the launch layer; a Pod from the
image (by `@sha256`, under the target security context) reaches Ready;
`GET /healthz` → 200; `GET /` → 200 (SSR + asset compile). Record PASS/FAIL +
exact errors in `docs/gate-zero.md`. FAIL → Approach C.

### Commit 1a — CNB pipeline alongside the old one

- New `pipeline/pipeline-cnb.yaml`: `(git-clone + SHA/contract pre-step)` →
  vendored `buildpacks` 0.6 (Paketo builder, `SKIP_RESTORE=true`, `dockerconfig`
  Secret, output to zot) → `smoke` → `deploy` by `@sha256`.
- New `scripts/test/e2e-cnb.sh` — asserts: `APP_IMAGE_DIGEST` is
  `sha256:<64hex>`; the `cv` Deployment image is `<zot>/cv@sha256:<digest>`;
  `imagePullPolicy` is `IfNotPresent`/`Always`; running pod `imageID` matches;
  deployed `/healthz` → 200; `cv-deploy-state` records url+digest+APP_SHA.
- New `scripts/test/negative-cnb.sh` — bad `frontend-ref` → pipeline fails, no
  build; pre-`/healthz` SHA → `buildpacks` succeeds, `smoke` fails, `deploy`
  skipped, teardown runs, nothing deployed.
- The old crane/apko pipeline stays; `validate.sh` covers both. `bootstrap.sh`
  gains a "seed zot + apply the CNB pipeline" path.
- **Demote the old design docs here:** `pipeline-restructure.md` and
  `pipeline-gitops-replan.md` get a `Status: SUPERSEDED by
  docs/designs/buildpacks-pivot.md` header and a one-line pointer. Their
  still-valid content is mapped in the table below.

### Commit 1b — delete the crane/apko pipeline

Delete `tasks/assemble.yaml`, `tasks/build.yaml`, `tasks/resolve.yaml` (folded),
`scripts/assemble-image.sh`, `scripts/resolve-sha.sh` + its bats,
`scripts/gen-digests.sh` (if unused by the CNB path), `scripts/check-toolchain.sh`,
`bootstrap/build-pipeline-utils.sh`, `bootstrap/pipeline-utils/`, the
`pipeline-utils` bootstrap phase, and the `params.yaml` / `digests.cue`
`pipelineUtils` entries. Delete the old `e2e.sh` / `negative.sh`; rename the
`-cnb` variants into place. `validate.sh` now covers one pipeline.

### Relationship to the old slice plan

| Old slice | Still valid? | Where it goes |
|---|---|---|
| Slice 0 (config + validation tooling) | yes | `digests.cue` + `validate.sh` kept; `gen-digests.sh` reviewed in 1b |
| Slice 1 (tracer bullet) | replaced | gate zero + Commit 1a/1b (CNB reference lane) |
| Slice 1.5 zot seed (replan, two-address) | yes | prerequisite for Commit 1a (needs a registry) |
| Slice 1.5 (per-stage SAs) | yes | a later commit, unchanged intent — now also splits the `dockerconfig`/`prepare-root` grants |
| Slice 2 (determinism) | **done (test + fix)** | `scripts/test/repro.sh` builds `cv_frontend@SHA` twice, asserts equal app-layer + SBOM-layer content hash (lifecycle's own `io.buildpacks.lifecycle.metadata` diffIDs) + equal outer digest (bonus). Root cause was `.git/` bleed-through, fixed in `fetch`. Repro CronJob deferred to TODOS.md (no persistent cluster yet — same gate as portability kind-CI). |
| Slice 3 (CVE gate + SBOM) | yes | CNB emits the SBOM (Syft/SPDX via Paketo); add the Trivy CVE gate; drop the old "Trivy authors, zot stores" split note (CNB authors now) |
| Slice 4 (Chains: build run + verify+deploy run) | **mechanism changed** | Chains observes `APP_IMAGE_URL`/`APP_IMAGE_DIGEST`. The full provenance chain needs the task to emit builder/buildpack/run-image digests as typed results — task-authoring work (Decision 8). Then the two-run split is unchanged. |
| Slice 5 (amd64 + multi-arch index) | yes | still gated on an amd64 node; Tekton Matrix over `arch`, one Paketo builder per arch |
| Slice 6 (promotion + zot authz) | yes | `crane cp` the verified digest, unchanged |
| Slice 7 (Timoni + Flux GitOps) | yes | unchanged |
| OpenBao slice | yes | unchanged |
| `plan/` reference (rules, glossary, test layers) | yes | migrate into a merged SSOT after Commit 1b |

## Open Questions

1. **Gate zero outcome** — RESOLVED 2026-09-01, PASS (`docs/gate-zero.md`,
   commit `a7897c1`). `heroku/builder:24` (arm64) builds `cv_frontend@cb333ee`
   end to end via the vendored `buildpacks` 0.6 task; the image serves `/healthz`
   and `/` with 200s; the `optionalDependencies` arm64 binaries survive the
   launch prune. **Approach A confirmed.**
2. **Node version control** — `BP_NODE_VERSION` env var vs tightening
   `cv_frontend`'s `engines.node`. Decide with the `cv_frontend` prep commit.
3. **`prepare` root step vs PSA** — does the pipeline namespace need `privileged`
   PSA, or can the root exception be scoped tighter? Check against the running
   cluster in Commit 1a.
4. **Reproducibility with CNB** — RESOLVED 2026-09-01, Slice 2. The lifecycle
   is already deterministic (SBOM layer + 10/12 layer diffIDs + gzip digests
   matched across two independent builds; `created` is pinned to
   `1980-01-01T00:00:01Z`). The lone drift was the app layer, from our `fetch`
   step leaving `.git/` for the buildpack to copy in. `fetch` now does
   `rm -rf .git`; builds are byte-for-byte reproducible. `docs/debt.md`
   (Resolved) + `scripts/test/repro.sh`.

## Deferred

- **The `cv/*` buildpack suite — `cv_packs`**
  (`github.com/InSuperposition/cv_packs`, empty repo created 2026-09-01; local
  scaffold at `../packs/`). Its own bisect-safe history, own design doc. Builds
  `cv_frontend` (and other fixtures) with hand-authored buildpacks, diffs against
  the stock heroku/builder:24 lane. **Never pinned into cv_oci's `BUILDER_IMAGE`.** Start it
  when the cv_oci curriculum is further along and you want the buildpack-authoring
  lesson. If it happens: `cv_packs/buildpacks/<id>/` where directory name ==
  buildpack id (`cv/node-engine`, `cv/npm-install`, `cv/npm-build`,
  `cv/npm-start`); lead with `npm-build` (the one with real logic). The 7 empty
  scaffold dirs (`artifacts build builders composites dependencies launch
  runtimes`) collide with CNB reserved terms — collapse to `buildpacks/`,
  `composites/`, `builders/`, `fixtures/`, `docs/`, `artifacts/` (gitignored).
- `cv/*` run image, image extensions, the public GHA/ghcr mirror — all `packs`
  concerns, and YAGNI until someone asks.
- amd64 / multi-arch index — still gated on a real amd64 node.

## Success Criteria (phased)

**Node prep (`cv_frontend` commit):** `engines.node` is bounded to 24.x;
`frontend-contract.md` updated; pinned fixture SHA bumped.

**Gate zero (Commit 0):** explicit PASS/FAIL in `docs/gate-zero.md` — the stock
Paketo arm64 builder builds `cv_frontend@SHA`, the image runs under the target
security context, `/healthz` + `/` return 200, `optionalDependencies` arm64
binaries are in the launch layer.

**Commit 1a:** `cv_frontend` deploys from a heroku/builder:24 CNB image pulled from
zot by `@sha256`; `e2e-cnb.sh` green; the old pipeline still green; old design
docs demoted; bisect-safe.

**Commit 1b:** one pipeline, `validate.sh` green, `e2e.sh` + `negative.sh` green,
no dead crane/apko files; bisect-safe.

**Slices:** each supply-chain slice lands as its own green, bisect-safe commit
per the table above.

**Signing (later):** `cosign verify` + `cosign verify-attestation` pass on the
release; the SLSA predicate names `cv_frontend@SHA` + the builder digest + the
buildpack group.

## Distribution Plan

- **App image:** built by the vendored `buildpacks` 0.6 task in-cluster, pushed to
  zot, deployed by digest by Tekton (`kubectl apply` now; Flux `OCIRepository`
  from the GitOps slice). No external distribution — same as today.
- **CI/CD:** the pipeline is its own CI/CD, `tkn`-triggered now; a Tekton Trigger
  on `cv_frontend` push stays a TODO (needs the signing slice stable first).

## Dependencies

- **`cv_frontend` Node-version prep** — blocks gate zero.
- **zot seed** (Slice 1.5 two-address plain-HTTP path) — blocks Commit 1a.
- **Gate zero result** — decides Approach A vs C.
- **Demote `pipeline-restructure.md` / `pipeline-gitops-replan.md`** — in Commit
  0 (CEO review), not later.

## Next Steps

**Status: T1, T2 (gate zero), and Commit 1a done — the CNB pipeline `cv-cnb`
runs green end to end alongside `cv-slice1`; `e2e-cnb.sh` (11/11) and
`negative-cnb.sh` pass. Builder is `heroku/builder:24` (Paketo has no arm64
build). zot pull path is one address, not two (OrbStack `k8s.expose_services`
+ daemon `insecure-registries`).**

1. **DONE — `cv_frontend` commit — Node pin.** `engines.node` → `>=24.3.0 <25`;
   `frontend-contract.md` + `digests.cue` bumped (`cv_frontend@cb333ee`).
2. **DONE — Commit 0 — gate zero record + demote old docs** (docs-only cv_oci commit).
   Run a throwaway TaskRun: vendored `buildpacks` 0.6 task + a stock Paketo arm64
   builder against `cv_frontend@SHA`; assert per Success Criteria. Commit:
   `docs/gate-zero.md` (the result), `Status: SUPERSEDED by
   docs/designs/buildpacks-pivot.md` headers on `pipeline-restructure.md` +
   `pipeline-gitops-replan.md`, and this doc's status. Decides A / C.
3. **DONE — Commit 1a — CNB pipeline alongside old.** `pipeline/pipeline-cnb.yaml`
   (`fetch → build → smoke → deploy`, resolve folded into fetch, inline
   smoke/deploy — no pipeline-utils), vendored `tasks/buildpacks.yaml` 0.6,
   `manifests/zot/` seed, `digests.cue` cnb* keys, `scripts/{smoke,deploy}.sh`
   pull-policy env, `scripts/test/{e2e-cnb,negative-cnb}.sh`, `bootstrap.sh`
   phase 5, `debt.md` + `runbook.md`. Commits `f77c9db` (+tests/bootstrap
   follow-up). No `dockerconfig` Secret — zot is unauth in the seed.
4. **DONE — Commit 1b — delete the crane/apko pipeline.** `tasks/{assemble,build,resolve}.yaml`,
   `pipeline/pipeline.yaml`, `scripts/assemble-image.sh`, `scripts/resolve-sha.sh`
   + its bats, `bootstrap/build-pipeline-utils.sh`, `bootstrap/pipeline-utils/`,
   `scripts/check-toolchain.sh`, `bootstrap.sh` phase 4, the `params.yaml` /
   `digests.cue` `nodeBuild` + `pipelineUtils` + `distrolessNode` entries,
   `scripts/{smoke,deploy}.sh` (fold into the CNB pipeline or keep as the
   inline logic's source), `scripts/test/{e2e,negative}.sh`. Rename `-cnb`
   tests into place. One green pipeline, `validate.sh` green.
5. **Slice commits** per the migration table:
   - **DONE — per-stage SAs.** `cv-pipeline-sa` split into `cv-build-sa` (no
     RBAC, no token) / `cv-smoke-sa` / `cv-deploy-sa`, wired via
     `taskRunSpecs`. Commit `d956572`.
   - **DONE — determinism.** `scripts/test/repro.sh` + the `.git`-strip fix in
     `fetch`. Open Question 4 resolved. Repro CronJob → TODOS.md.
   - next: CVE + SBOM → Chains (with the provenance-authoring work) →
     multi-arch → promotion → GitOps → OpenBao.
   Merge `pipeline-restructure.md`'s reference material into a single SSOT
   after 1b.
6. **`packs` project** — separate, later, its own design doc.

## What I noticed about how you think

- You said *"the most important thing is to keep it simple, zot and other parts
  of the stack should be reused, we can fix things at a later date."* When the
  eng review's outside voice argued the buildpack suite was scope creep, you took
  the decouple in one move — no sunk-cost defense of a doc you'd just approved.
- When Codex flagged the arm64 risk you said *"first research more, and think of
  mitigations, even if we have to build for amd64 instead of arm, simpler is
  better."* You held the goal constant and let the method flex.
- You stopped me mid-question to say *"there are already folders in packs folder,
  check and confirm naming is correct."* You'd rather I verify against what
  exists than pick from options I made up.
- *"bisect safe, correct naming conventions"* was your answer to a question about
  project goals. Most people answer with a feature. You answered with a property
  of the history.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 3 | issues_open (folded) | this run: HOLD_SCOPE — decouple confirmed, `cv_packs` deferred (option A), doc demotion moved to Commit 0; 3 AUQ decisions answered. Prior runs (2026-09-01) graded the superseded crane/apko re-plan. |
| Codex Review | `/codex review` | Independent 2nd opinion | 2 | issues_found | Codex `exec` timed out on this repo every attempt (5-min, ~0 output — logged); Claude subagent ran both times |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 3 | issues_open (folded) | SCOPE_REDUCED — 13 findings, 1 critical gap (Node-25 resolution, closed by task T1); all 10 AUQ decisions answered |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | n/a (infra) |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CODEX:** `codex exec` stalled on this repo every attempt (5-min timeout, ~0 output) — logged learning `codex-exec-stalls-cv-oci-confirmed`. Claude subagent outside voices ran instead (16 findings at eng review).
- **CROSS-MODEL:** office-hours Codex cold-read and the eng-review outside voice agree the reference lane + slice migration is the real portfolio piece and disagree with the original office-hours doc on the `cv/*` buildpack suite. The user adopted the decouple; the CEO review confirmed it (`cv_packs` deferred, not killed, not capstoned).
- **VERDICT:** CEO + ENG REVIEW COMPLETE — HOLD SCOPE, plan revised and confirmed. Not "clean" (1 eng critical gap with fix task T1). Ready to implement: T1 (Node pin) → T2 (gate zero + doc demotion). Design review not required (infra). Next action is code, not another review.

NO UNRESOLVED DECISIONS
