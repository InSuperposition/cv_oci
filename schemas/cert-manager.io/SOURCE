cert-manager.io v1 CRD schemas (Certificate, Issuer, ClusterIssuer,
CertificateRequest) for kubeconform, vendored so validate.sh stays offline.

Source: github.com/datreeio/CRDs-catalog `cert-manager.io/` (same catalog as
schemas/tekton.dev/). Files renamed to kubeconform's
`<ResourceKind>_<ResourceAPIVersion>.json` convention.

Pinned alongside vendor/cert-manager/cert-manager-v1.21.1.yaml. Regenerate on a
cert-manager version bump.
